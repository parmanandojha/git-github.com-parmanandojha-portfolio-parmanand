# Fonts

The site loads **`src/P.woff`** via `@font-face` in `src/index.css` (Neue Montreal family name for CSS variables).

You no longer need files in this folder for the default setup. Optional: add extra weights or formats here if you expand later.
